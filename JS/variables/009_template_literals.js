const user1 = "magesh";
const url = `${user1}/users`;
console.log(url);

//Build /users/101 using a userId variable.
const userId = 234;
const result_endpoint = `/users/${userId}`;
console.log(result_endpoint);

//Build a message saying Hello <name> using a variable.

const name = "Suresh";
console.log(`Hello ${name}`);
