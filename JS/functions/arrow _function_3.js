// Write an arrow function that converts a string to uppercase.

const convertToUpperCase = (test_string) => {
    return test_string.toUpperCase();
}

let result = convertToUpperCase("hello world")
console.log(result)