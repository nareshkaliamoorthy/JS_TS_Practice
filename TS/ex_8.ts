//Functions
//Q1:
function getResponseTime(endpoint: string, time: number): string {
    return "ADDED RETURN PLACEHOLDER TO AVOID COMPLIE TIME ERROR";
}

//Q2:

function isTestPass(status: string): boolean {
    if (status === "PASS") {
        return true;
    } else {
        return false;
    }

}

//Q3: This will have a compile error because we are returning a number as per function syntax but there is no return statement within the code block
